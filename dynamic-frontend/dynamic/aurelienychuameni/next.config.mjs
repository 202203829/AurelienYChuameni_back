/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    cssModules: true, // Activa los módulos CSS si los estás usando
  };
  
  export default nextConfig;
  
